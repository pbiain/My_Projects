# Apart Club San Pedro — Session Log
**Date:** March 4, 2026

---

## Bug Fixes

1. **Fixed missing `import threading`** in `server.py`
   - Was crashing on every HOT lead notification
   - File: `server.py`

2. **Fixed Gmail SMTP timeout**
   - `smtplib.SMTP_SSL` port 465 was hanging 30s and killing gunicorn workers
   - Added `timeout=5` and credentials guard to skip gracefully if env vars not set
   - File: `apart_agent/nodes/send_gmail.py`

3. **Fixed Gmail port (465 → 587)**
   - Railway blocks outbound port 465
   - Switched to port 587 with STARTTLS (`smtplib.SMTP` + `server.starttls()`)
   - Confirmed working: multiple WARM lead emails received in inbox
   - File: `apart_agent/nodes/send_gmail.py`

4. **Fixed wrong lot prices**
   - RAG was returning `$45/m²` from a conflicting investment proposal PDF
   - Hardcoded verified prices (`$120/m²`, E1–F5 table) in system prompt as ground truth
   - File: `apart_agent/nodes/react_agent.py`

---

## Code Quality

5. **Renamed `mercadolibre_tool.py` → `prospect_search_tool.py`**
   - Tool uses DuckDuckGo, not MercadoLibre — name was misleading
   - Updated import in `react_agent.py`

6. **Cleaned README**
   - Replaced all Tavily references with DuckDuckGo + Hunter.io
   - Tavily was removed from production weeks ago, README was outdated

---

## Prompt Engineering

7. **Tightened system prompt** (`apart_agent/nodes/react_agent.py`)
   - Added: "YOUR ONLY JOB: Sell Apart Club San Pedro"
   - Hard rules: never mention competitors, never suggest other options
   - Redirect all general market questions to the project's own offering

8. **Embedded verified lot prices in system prompt**
   - Full E1–F5 lot table with exact prices and availability status
   - Overrides any conflicting RAG results
   - Available lots: E1, E2, E3, E4, F4, F5

---

## Data / Pinecone

9. **Identified conflicting documents** in `pinecone_docs/`:
   - `Propuesta de inversión.pdf` — had wrong `$45/m²` stage 2 pricing
   - `PROPUESTA Actualizada xa Andrés-Enero_26.pdf` — personal proposal, not for public chatbot

10. **Deleted both conflicting PDFs** from repo and folder

11. **Renamed `REGLAMENTO EDIFICACIÓN-BOATING I.pdf`** → `REGLAMENTO EDIFICACION-BOATING I.pdf`
    - Accented filename caused Windows cp1252 encoding crash in `ingest.py`

12. **Cleared Pinecone and re-ingested**
    - 2 clean documents, 69 chunks total
    - `REGLAMENTO EDIFICACION-BOATING I.pdf`
    - `VALORES DE LOTES EN BOATING I.docx.pdf`

---

## n8n / Integrations

13. **Fixed n8n timestamp format**
    - Changed `={{$now}}` (ISO 8601) → `={{$now.toFormat('dd/MM/yyyy HH:mm:ss')}}`
    - Applied to both Google Sheets nodes (Inbound leads + Outbound Prospects)
    - File: `N8N/Apart Club — Python Agent.json` (re-import into n8n required)

---

## Railway Deployment

14. **Deployed and verified end-to-end** on `myprojects-production-7535.up.railway.app`:
    - HOT lead → Telegram alert ✅
    - WARM lead → Gmail notification ✅
    - All leads → Google Sheets via n8n webhook ✅
    - N8N_WEBHOOK_URL: `https://magical-automations.app.n8n.cloud/webhook/apart-agent`

---

## Git Commits (pushed to main)

| Commit | Description |
|--------|-------------|
| `ccaa95d` | Fix: switch Gmail SMTP from port 465 (SSL) to port 587 (STARTTLS) |
| `ced61a3` | Fix: skip Gmail if credentials not set |
| `63dea52` | Fix: add 5s timeout to Gmail SMTP |
| `927c0b8` | Docs: replace Tavily with DuckDuckGo + Hunter.io |
| `4679c06` | Rename: mercadolibre_tool → prospect_search_tool |
| `807a7ae` | Fix: tighten system prompt to exclusively sell Apart Club San Pedro |
| `8f885b6` | Fix: embed verified lot prices in system prompt |
| `a66f06c` | Cleanup: remove conflicting docs, fix n8n timestamp format |
