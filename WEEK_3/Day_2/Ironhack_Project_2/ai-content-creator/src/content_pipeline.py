"""document → monitor → brief → publish → iterate pipeline."""


class ContentPipeline:
    """Coordinates the end-to-end content generation workflow."""

    def run(self) -> None:
        print("Pipeline initialized: document → monitor → brief → publish → iterate")
